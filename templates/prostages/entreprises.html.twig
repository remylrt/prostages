{% extends "templateProstages.html.twig" %}

{% block title %}Entreprises{% endblock %}

{% block body %}
<div class="panel panel-default">
    <div class="panel-heading"><h4>Entreprises</h4></div>
    <ul class="list-group">
        {% for entrepriseCourante in entreprises %}
            <li class="list-group-item">
                <a href="#">{{entrepriseCourante.nom}}</a>
            </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}