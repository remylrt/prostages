{% extends "templateProstages.html.twig" %}

{% block title %}Ajouter unn stage{% endblock %}

{% block body %}
    <div class="panel-heading"><h4>Ajout d'un stage</h4></div>
    {{ form_start(vueFormulaireStage, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(vueFormulaireStage) }}
        <button type="submit" class="btn btn-primary">Ajouter</button>
    {{ form_end(vueFormulaireStage) }}
{% endblock %}